Run the full build pipeline to compile all Typst SOPs to PDF and generate combined PDFs.

Execute `./build.sh` from the repo root. Report any compilation errors with the specific file and error message. On success, summarize what was built (number of individual PDFs + combined PDFs).
